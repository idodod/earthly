{
  $schema: 'https://docs.renovatebot.com/renovate-schema.json',
  extends: [
    'config:recommended',
    'default:pinDigestsDisabled',
  ],
  configMigration: true,
  prHourlyLimit: 1,
  schedule: [ // utc timezone
    'after 6am on monday',
  ],
  includePaths: [
    'docs/**',
  ],
  enabledManagers: [
    'regex',
  ],
  regexManagers: [
    {
      // Earthly Version in md files
      customType: 'regex',
      fileMatch: [
        '.+\.md',
      ],
      matchStrings: [
        'earthly/releases/download/(?<currentValue>v\\d+\\.\\d+\\.\\d+)/',
        '- uses: earthly/actions/setup-earthly@.*?[\\s\\n]+.*?[\\s\\n]+with:[\\s\\n]*version: (?<currentValue>v\\d+\\.\\d+\\.\\d+)[\\s\\n/]+',
        '\'earthly/earthly:(?<currentValue>v\\d+\\.\\d+\\.\\d+)\'',
//        'github.com/earthly/earthly:(?<currentValue>.+?)\\+',
  //      'earthly/earthly:(?<currentValue>.+?)[\\s\\n/]+',
      ],
      depNameTemplate: 'earthly/earthly',
      datasourceTemplate: 'github-releases',
    },
  ],
  labels: [
    'renovate',
  ],
}
